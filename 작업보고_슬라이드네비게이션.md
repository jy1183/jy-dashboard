# 대시보드 슬라이드 네비게이션 + 트렐로 칸반 보드 뷰 작업 보고

## 작업일: 2026-04-28

---

## 1. 배경 및 문제점

현재 WELLASSET BOARD 대시보드(https://dashboard-deploy-seven.vercel.app/)는 구글 캘린더, 트렐로 체크리스트, 부동산 뉴스, LH 매입공고를 한 화면에서 볼 수 있도록 구성되어 있다. 트렐로의 체크리스트를 보드 통합 형태로 확인하고 완료 처리 및 각 카드 접근까지는 가능하나, 트렐로 보드 자체를 확인하려면 별도로 트렐로 앱이나 웹에 접속해야 하는 불편함이 있었다.

## 2. 요청사항

대시보드 내에서 창 전환(슬라이드 형태)을 통해 트렐로 웹에 바로 접근할 수 있는 기능 구현. iframe 등을 통한 트렐로 웹 임베드 방식도 검토 요청.

## 3. 기술 검토

### iframe 임베드 방식 (불가)
트렐로는 `X-Frame-Options` 보안 헤더를 설정하여 외부 사이트에서 iframe으로 직접 임베드하는 것을 차단한다. 따라서 트렐로 웹을 iframe으로 넣는 방식은 사용할 수 없다.

### 채택된 대안: API 기반 칸반 보드 뷰
기존에 구축된 트렐로 API 연동을 활용하여 대시보드 내에 칸반 스타일 보드 뷰를 직접 구축하는 방식을 채택했다. 이미 설치되어 있는 framer-motion 라이브러리를 활용하여 슬라이드 전환 애니메이션을 구현한다.

## 4. 작업 계획

| 단계 | 작업 내용 | 상태 |
|------|-----------|------|
| 1 | 프로젝트 구조 및 코드 분석 | 완료 |
| 2 | Board API 동적 보드 ID 지원 확장 | 완료 |
| 3 | 슬라이드 네비게이션 시스템 구축 | 완료 |
| 4 | 트렐로 칸반 보드 뷰 구현 | 완료 |
| 5 | 빌드 테스트 및 검증 | 완료 |
| 6 | 배포 (git push → Vercel 자동 배포) | 완료 |
| 7 | UI 버그 수정 (네비 버튼 겹침 이슈) | 완료 |
| 8 | 카드 제목 링크 표시 수정 (URL → 파일명) | 완료 |
| 9 | 트렐로 알림(Bell) 기능 추가 | 완료 |
| 10 | 알림창 글자 크기 10% 확대 | 완료 |
| 11 | 알림창 외부 클릭 시 닫기 | 완료 |
| 12 | 기한 지난 할일 컬럼 추가 | 완료 |
| 13 | 트렐로 보드 Activity 패널 고정 | 완료 |
| 14 | Shift+휠 페이지 전환 | 완료 |
| 15 | Activity 자동 로딩 | 완료 |

## 5. 수정 파일 및 변경 내용

### 5-1. `app/api/trello/board/route.ts`
**변경 사항**: 동적 보드 조회 지원

- 기존: 보드 ID가 `XOH8XjzB`(준비)로 하드코딩
- 변경: 쿼리 파라미터 `?boardId=xxx`로 모든 보드를 동적으로 조회 가능
- 추가 데이터: 보드 이름(`boardName`), 보드 URL(`boardUrl`), 카드 badges(체크리스트 수, 댓글 수, 첨부파일 수) 반환

### 5-2. `app/page.tsx`
**변경 사항**: 슬라이드 네비게이션 + 칸반 보드 뷰 전면 개편

주요 구현 내용:

- **슬라이드 전환 네비게이션**: framer-motion `AnimatePresence`를 활용한 좌우 슬라이드 전환
  - 페이지 0: 기존 대시보드 (캘린더, 할일, 뉴스, LH공고)
  - 페이지 1: 트렐로 칸반 보드 뷰
  - 키보드 좌우 화살표 키로도 전환 가능

- **트렐로 칸반 보드 뷰**: 4개 보드 탭 전환 지원
  - 동천동 (`zHDWraQl`)
  - 기타 (`yFCQoAY5`)
  - 준비 (`XOH8XjzB`)
  - 법인 (`p4hR5CFc`)
  - 리스트별 카드 표시 (Kanban 스타일)
  - 카드 라벨 색상 표시
  - 마감일 배지 (기한 초과 시 빨간색)
  - 체크리스트 진행률 배지
  - 댓글/첨부파일 수 배지
  - 카드 클릭 시 트렐로 팝업 열기
  - 보드별 새로고침 버튼
  - 트렐로 웹에서 열기 링크 (ExternalLink 아이콘)

- **대시보드 헤더 개선**:
  - WELLASSET BOARD 타이틀 우측에 "트렐로 보드 >" 전환 버튼 배치
  - 하단 보드 바로가기 버튼(동천동/기타/준비/법인) 클릭 시 해당 보드의 칸반 뷰로 직접 이동

- **보드 설정 상수화**: `TRELLO_BOARDS` 배열로 보드 정보를 상단에 통합 관리

### 5-3. `app/page.tsx` (2차 개선 — 단계 8~15)
**변경 사항**: UX 개선 및 기능 확장 (444줄)

- **카드 제목 링크 표시 수정**: 카드 이름이 URL인 경우 첨부파일의 실제 파일명으로 표시하고, Link 아이콘으로 구분. 파일명 매칭이 안 되면 URL을 2줄까지 축약 표시
- **트렐로 알림(Bell) 기능**: 보드 뷰 헤더에 알림 벨 아이콘 추가. 클릭 시 드롭다운으로 최근 25개 알림 표시, 읽지 않은 알림 개수 배지, "모두 읽음" 처리
- **알림창 글자 크기 10% 확대**: 제목 `text-[14px]`, 내용 `text-[13px]`, 보드명/시간 `text-[12px]`, 헤더 `text-[17px]`
- **알림창 외부 클릭 시 닫기**: `useRef` + `mousedown` 이벤트로 드롭다운 외부 클릭 감지
- **기한 지난 할일 컬럼**: "오늘" 왼쪽에 빨간색 테마의 "기한 지남" 컬럼 표시. API에 `?overdue=true` 파라미터로 과거 30일 내 미완료 항목 조회. 항목이 없으면 컬럼 자동 숨김
- **Activity 패널**: 트렐로 보드 뷰 우측에 280px 고정 Activity 패널 상시 표시. 보드 전환 시 해당 보드의 활동 내역 자동 갱신. 멤버명, 액션 유형, 카드명, 리스트 이동, 체크아이템 상태 표시
- **Activity 자동 로딩**: 보드 페이지 진입 시 Activity 데이터 자동 fetch
- **Shift+휠 페이지 전환**: Shift 키 + 마우스 휠로 대시보드 ↔ 트렐로 보드 슬라이드 전환
- **Activity 패널 스타일**: 헤더 배경색 진하게(`bg-slate-200/80`), 글자 크기 110% 적용

### 5-4. `app/api/trello/activity/route.ts` (신규)
**변경 사항**: 보드 활동 내역 API

- GET: `/api/trello/activity?boardId=xxx&limit=30`
- Trello `/1/boards/{boardId}/actions` 호출
- 멤버명, 카드명, 리스트명, 댓글 텍스트, 체크아이템 정보 파싱 후 반환

### 5-5. `app/api/trello/checklists/route.ts` (수정)
**변경 사항**: 기한 지난 할일 지원

- `?overdue=true` 파라미터 추가
- 과거 30일 내 미완료 체크아이템을 `overdueTasks` 배열로 별도 반환
- 기한 지난 항목은 `dayIndex: -1`로 구분

### 5-6. `app/api/trello/notifications/route.ts` (신규)
**변경 사항**: 트렐로 알림 API

- GET: `/api/trello/notifications?limit=25` — 알림 목록 + 읽지 않은 개수 반환
- PUT: `/api/trello/notifications` — 모든 알림을 읽음 처리

### 5-7. `app/globals.css`
**변경 사항**: 칸반 보드 전용 스타일 추가

- `.kanban-list`: 리스트 컨테이너 스타일
- `.kanban-card`: 카드 hover 시 `translateY(-1px)` 부양 효과
- 칸반 리스트 내 커스텀 스크롤바 (4px 너비, 반투명)

## 6. 기술 스택

- Next.js 16.1.1 (기존)
- React 19.2.3 (기존)
- Tailwind CSS 4.1.18 (기존)
- framer-motion 12.38.0 (기존 설치, 이번 작업에서 활용)
- lucide-react 0.577.0 (기존, `Trello`, `ArrowLeft`, `ExternalLink`, `RefreshCw`, `ChevronRight`, `Bell`, `Link`, `Clock` 아이콘 사용)
- Vercel 배포 (기존)

## 7. 커밋 이력

| 커밋 | 설명 |
|------|------|
| `1c22de7` | feat: slide nav + trello kanban board view (3 files, +582/-274) |
| — | fix: remove duplicate nav, header inline only |
| — | feat: card link display fix + notification bell |
| — | feat: 5 improvements - notification size, click-outside, overdue tasks, activity panel, shift+wheel nav |
| — | style: activity panel header darker + font size 110% |
| — | fix: auto-fetch activity on board page enter |

## 8. 알려진 이슈 및 후속 작업

- 칸반 보드에서 카드 이동(드래그앤드롭) 기능은 미구현 (현재는 조회 + 클릭 팝업만 지원)
- 보드 데이터 캐싱 미적용 (매번 API 호출)
